server:
  port: 8080

spring:
  application:
    name: service-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/finance
    username: admin
    password: 1234

  data:
    redis:
      host: localhost
      port: 6379